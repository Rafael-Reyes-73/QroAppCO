// App.js (simplificado como en tu otro proyecto)
import React from 'react';
import LoginScreen from './screens/LoginScreen';

export default function App() {
  // Solo para pruebas, pero NO se usa porque Expo Router es el punto de entrada
  return <LoginScreen />;
}
